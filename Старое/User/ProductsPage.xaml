<Page x:Class="User.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:User"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductsPage">
    <GroupBox Header="Товары" Grid.Column="1" Margin="15" Padding="5" FontWeight="SemiBold" Background="#F0F2F5" FontSize="14">
    <Grid Background="#F9F9F9">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <!-- Боковая панель фильтров -->
            <ColumnDefinition Width="*"/>
            <!-- Контент -->
        </Grid.ColumnDefinitions>
        <!-- 1. Боковая панель -->
        <StackPanel Grid.Column="0" Background="White" Margin="10">
            <TextBlock Text="Поиск" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Height="30" VerticalContentAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="Цена от" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding MinPrice, UpdateSourceTrigger=PropertyChanged,TargetNullValue=''}" 
             Height="30" VerticalContentAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Категория" FontWeight="Bold" Margin="0,10,0,5"/>
            <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="Name" Height="30" Margin="0,0,0,20">
            </ComboBox>
            <TextBlock Text="Сортировка" FontWeight="Bold" Margin="0,0,0,5"/>
            <!-- Добавлен UpdateSourceTrigger -->
            <ComboBox SelectedValue="{Binding SortBy, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Tag" Height="30" Margin="0,0,0,20">
                <ComboBoxItem Content="Новинки" Tag="CreatedAtDesc"/>
                <ComboBoxItem Content="Сначала дешевые" Tag="PriceAsc"/>
                <ComboBoxItem Content="Сначала дорогие" Tag="PriceDesc"/>
                <ComboBoxItem Content="По названию (А-Я)" Tag="NameAsc"/>
            </ComboBox>
        </StackPanel>
        <!-- 2. Основная область -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <!-- Сетка товаров -->
                <RowDefinition Height="Auto"/>
                <!-- Пагинация -->
            </Grid.RowDefinitions>
            <!-- Сетка товаров -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Products}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Left"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="200" Height="380" Margin="10" Background="White" CornerRadius="10">
                                    <Grid>
                                        <StackPanel>
                                            <!-- Картинка + Кнопка Избранное -->
                                            <Grid>
                                                <Border CornerRadius="10,10,0,0" Background="Transparent" ClipToBounds="True">
                                                    <Image Source="{Binding MainImageUrl}" Height="160" Stretch="UniformToFill"/>
                                                </Border>
                                                <!-- Кнопка "Сердечко" -->
                                                <Button Click="BtnFavorite_Click" Tag="{Binding Id}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" Width="50" Height="50" Background="Yellow"  BorderThickness="0" Cursor="Hand">
                                                    <!-- Скругление кнопки -->
                                                    <Button.Resources>
                                                        <Style TargetType="Border">
                                                            <Setter Property="CornerRadius" Value="16"/>
                                                        </Style>
                                                    </Button.Resources>
                                                    <!-- Содержимое кнопки (Сердечко) -->
                                                    <Button.Content>
                                                        <TextBlock Text="❤" FontSize="20" Foreground="LightGray" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Button.Content>
                                                    <Button.Effect>
                                                        <DropShadowEffect BlurRadius="5" ShadowDepth="1" Opacity="0.3"/>
                                                    </Button.Effect>
                                                </Button>
                                            </Grid>
                                            <!-- Описание (Margin не меняем) -->
                                            <StackPanel Margin="12,8,12,12">
                                                <TextBlock Text="{Binding Brand}" Foreground="Gray" FontSize="11" Margin="0,0,0,2"/>
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" Height="40" TextWrapping="Wrap" VerticalAlignment="Top"/>
                                                <!-- Просмотры -->
                                                <StackPanel Orientation="Horizontal" Margin="0,5">
                                                    <TextBlock Text="&#xE890;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,5,0"/>
                                                    <TextBlock Text="{Binding ViewsCount}" FontSize="12" VerticalAlignment="Center" Foreground="DimGray"/>
                                                    <TextBlock Text=" просмотров" FontSize="12" VerticalAlignment="Center" Foreground="DimGray" Margin="3,0,0,0"/>
                                                </StackPanel>
                                                <TextBlock Text="{Binding PurchaseCount, StringFormat='Продано: {0}'}" FontSize="14" HorizontalAlignment="Left"/>
                                                <TextBlock Text="{Binding MinPrice, StringFormat={}{0:N0} ₽}" FontSize="18" Foreground="#E64A19" FontWeight="Black" Margin="0,8"/>
                                                <Button Content="Подробнее" Tag="{Binding Id}" Click="BtnDetails_Click" Height="30" Cursor="Hand"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
            </ScrollViewer>
            <!-- Пагинация -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <Button Content="Назад" Command="{Binding PrevPageCommand}" Width="80" Height="35"/>
                <TextBlock VerticalAlignment="Center" Margin="20,0">
                    <Run Text="{Binding CurrentPage}"/>
                    <Run Text=" из "/>
                    <Run Text="{Binding TotalPages}"/>
                </TextBlock>
                <Button Content="Вперед" Command="{Binding NextPageCommand}" Width="80" Height="35"/>
            </StackPanel>
        </Grid>
    </Grid>
</GroupBox >
</Page>
