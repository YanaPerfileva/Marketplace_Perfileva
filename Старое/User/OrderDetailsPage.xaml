<Page x:Class="User.OrderDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:User"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderDetailsPage">
    <GroupBox Header="История заказов" Grid.Column="1" Margin="15" Padding="5" FontWeight="SemiBold" FontSize="14">
        <Grid Background="#F0F2F5">
        <StackPanel Margin="10" >
            <Button Content="← К списку заказов" Click="Back_Click" HorizontalAlignment="Left" Margin="0,0,0,15"/>
            <TextBlock Text="{Binding OrderNumber, StringFormat='Детали заказа №{0}'}" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding Status, StringFormat='Статус: {0}'}" Foreground="Gray" Margin="0,0,0,20"/>
                <!-- Список товаров в заказе -->
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding OrderItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="70"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- Картинка товара с "заглушкой", если фото нет -->
                                        <Grid Grid.Column="0">
                                            <Border Width="60" Height="60" Background="#E0E0E0" CornerRadius="8"/>
                                            <Image Source="{Binding ProductSku.Product.ImageUrl}" Width="60" Height="60" Stretch="UniformToFill"/>
                                        </Grid>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                                            <TextBlock Text="{Binding ProductSku.Product.Name}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding ProductSku.Size, StringFormat='Размер: {0}'}" FontSize="12" Foreground="Gray" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding ProductSku.Color, StringFormat='Цвет: {0}'}" FontSize="12" Foreground="Gray"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Quantity, StringFormat='Кол-во: {0} шт.'}" FontSize="12" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" Text ="{Binding ProductSku.Price, StringFormat='{}{0:N0} ₽'}" 
                                   VerticalAlignment="Center" FontWeight="Bold" Foreground="#2E7D32" FontSize="16"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
    </Grid>
 </GroupBox>
</Page>
