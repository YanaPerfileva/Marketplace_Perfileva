<Page x:Class="User.FavoritesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:User"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FavoritesPage">
    <GroupBox Header="Мои избранные товары" Grid.Column="1" Margin="15" Padding="5" FontWeight="SemiBold" Background="#F0F2F5" FontSize="16">
    <Grid Background="#F0F2F5">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel >
                    <!-- Список избранного -->
                <ItemsControl x:Name="FavoritesList" ItemsSource="{Binding Favorites}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="12" Margin="0,0,0,15" Padding="15" BorderBrush="#EEE" BorderThickness="1">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Opacity="0.1"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <!-- Иконка товара -->
                                        <ColumnDefinition Width="*"/>
                                        <!-- Описание -->
                                        <ColumnDefinition Width="Auto"/>
                                        <!-- Цена и Кнопка -->
                                    </Grid.ColumnDefinitions>
                                    <!-- 1. Заглушка под фото товара -->
                                    <Border Grid.Column="0" Width="60" Height="60" Background="#F0F0F0" CornerRadius="8" Margin="0,0,15,0">
                                        <TextBlock Text="📦" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <Image Source="{Binding Product.ImageUrl}" Width="50" Height="50" Margin="0,0,15,0"/>
                                        <!-- 2. Данные ТОВАРА (через Include Product) -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Product.Name}" FontSize="16" FontWeight="SemiBold" Foreground="#2C3E50"/>
                                            <TextBlock Text="{Binding Product.Category}" FontSize="12" Foreground="Gray" Margin="0,2,0,0"/>
                                            <TextBlock Text="{Binding CreatedAt, StringFormat='Добавлено: {0:dd.MM.yyyy}'}" FontSize="10" Foreground="Silver" Margin="0,5,0,0"/>
                                        </StackPanel>
                                        <!-- 3. Цена и действие -->
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="10,0,0,0">
                                            <TextBlock Text="{Binding Product.Price, StringFormat='{}{0:N2} ₽'}" FontSize="18" FontWeight="Bold" Foreground="#E74C3C" HorizontalAlignment="Right"/>
                                            <Button Content="В корзину" 
            Click="AddToCart_Click" 
            Tag="{Binding Product.Id}"
            Background="#27AE60" Foreground="White" 
            Padding="10,5" Margin="0,5" BorderThickness="0" Cursor="Hand">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius" Value="4"/>
                                                    </Style>
                                                </Button.Resources>
                                            </Button>
                                            <Button Content="Удалить" Click="DeleteFavorite_Click" CommandParameter="{Binding Id}" Background="Transparent" Foreground="Gray" BorderThickness="0" Cursor="Hand" Margin="0,5,0,0" FontSize="11" HorizontalAlignment="Right">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline"/>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <!-- Заглушка, если избранного нет -->
                    <TextBlock Text="В избранном пока ничего нет" HorizontalAlignment="Center" Margin="0,50,0,0" Foreground="Gray">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <!-- По умолчанию скрываем текст -->
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <!-- Если количество элементов равно 0, показываем текст -->
                                    <DataTrigger Binding="{Binding Favorites.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
        </ScrollViewer>
    </Grid>
 </GroupBox>
</Page>
