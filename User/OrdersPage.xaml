<Page x:Class="User.OrdersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:User"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrdersPage">
    <GroupBox Header="История заказов" Grid.Column="1" Margin="15" Padding="5" FontWeight="SemiBold" Background="#F0F2F5" FontSize="14">
        <DataGrid x:Name="DGridUser" ItemsSource="{Binding Orders}" 
              MouseDoubleClick="DGridUser_MouseDoubleClick" 
              AutoGenerateColumns="False" 
              IsReadOnly="True" 
              CanUserAddRows="False"
              Background="White"
              GridLinesVisibility="Horizontal"
              HorizontalGridLinesBrush="#CCCCCC"
              HeadersVisibility="Column"
              BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding OrderNumber}" Width="*" ElementStyle="{StaticResource CellCenter}"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="*" ElementStyle="{StaticResource CellCenter}"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding TotalPrice,  StringFormat={}{0:N0} ₽}" Width="*" >
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DiscountAmount}">
                                    <Setter Property="Foreground" Value="Gray"/>
                                    <Setter Property="TextDecorations" Value="Strikethrough"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Скидка" Binding="{Binding DiscountDisplay}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" >
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="Crimson"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Итого" Binding="{Binding FinalPrice, StringFormat={}{0:N0} ₽}" Width="*" ElementStyle="{StaticResource CellCenter}"  />
                <DataGridTextColumn Header="Дата" Binding="{Binding CreatedAt, StringFormat='dd.MM.yyyy HH:mm'}" Width="*" ElementStyle="{StaticResource CellCenter}"/>
                <DataGridTextColumn Header="Комментарий" Binding="{Binding Comment}" Width="*" ElementStyle="{StaticResource CellCenter}"/>
            </DataGrid.Columns>
        </DataGrid>
    </GroupBox>
</Page>
